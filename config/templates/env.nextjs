NODE_ENV=production
PORT=${PORT}
NEXT_PUBLIC_API_URL=https://${DOMAIN}/api

# Database
DATABASE_URL="${DB_CONNECTION}://${DB_USER}:${DB_PASSWORD}@localhost:${DB_PORT}/${DB_NAME}"

# Add your environment variables here
